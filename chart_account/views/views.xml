<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="chart_account.list">
      <field name="name">chart_account list</field>
      <field name="model">chart_account.chart_account</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="chart_account.action_window">
      <field name="name">chart_account window</field>
      <field name="res_model">chart_account.chart_account</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="chart_account.action_server">
      <field name="name">chart_account server</field>
      <field name="model_id" ref="model_chart_account_chart_account"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="chart_account" id="chart_account.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="chart_account.menu_1" parent="chart_account.menu_root"/>
    <menuitem name="Menu 2" id="chart_account.menu_2" parent="chart_account.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="chart_account.menu_1_list" parent="chart_account.menu_1"
              action="chart_account.action_window"/>
    <menuitem name="Server to list" id="chart_account" parent="chart_account.menu_2"
              action="chart_account.action_server"/>
    -->
  </data>
</odoo>